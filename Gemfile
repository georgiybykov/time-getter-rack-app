# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.7.2'

# The interface for developing web applications in Ruby.
gem 'rack'

# Logging messages in the program.
gem 'logger'
